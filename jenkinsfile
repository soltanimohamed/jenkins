pipeline{
    agent any
    stages{
        stage('Deploy petstore'){
            steps{
                sh "docker-compose -f petstore.yml up -d"
            }
        }
        stage('remove'){
            steps{
                sh "docker stop pet"
            }
        }
    }
}
